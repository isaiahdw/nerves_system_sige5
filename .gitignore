# Mix/Nerves artifacts
/_build/
/deps/
/.nerves/
*.fw
build.log
/log/
/archive.log

# Experimental bootloader from scripts/build-uboot.sh. u-boot-rockchip.bin is
# tracked because fwup packages it; this one carries a secure world nothing has
# qualified yet, and is rebuilt on demand.
/uboot/u-boot-rockchip-ostee.bin
/uboot/optee-ta/
