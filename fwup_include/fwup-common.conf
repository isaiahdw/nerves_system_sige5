# Firmware metadata and disk layout shared by fwup.conf and fwup-ops.conf
#
# All of these can be overridden using environment variables of the same name.
#
#  Run 'fwup -m' to query values in a .fw file.
#  Use 'fw_printenv' to query values on the target.
#
# These are used by Nerves libraries to introspect.
define(NERVES_FW_PRODUCT, "Nerves Firmware")
define(NERVES_FW_DESCRIPTION, "")
define(NERVES_FW_VERSION, "${NERVES_SDK_VERSION}")
define(NERVES_FW_PLATFORM, "sige5")
define(NERVES_FW_ARCHITECTURE, "aarch64")
define(NERVES_FW_AUTHOR, "Isaiah")

# The soldered eMMC (the boot device).
define(NERVES_FW_DEVPATH, "/dev/mmcblk0")
define(NERVES_FW_APPLICATION_PART0_DEVPATH, "/dev/mmcblk0p4") # Linux part number is 1-based
define(NERVES_FW_APPLICATION_PART0_FSTYPE, "f2fs")
define(NERVES_FW_APPLICATION_PART0_TARGET, "/root")
define(NERVES_PROVISIONING, "${NERVES_SYSTEM}/images/fwup_include/provisioning.conf")

# Default paths if not specified via the commandline
define(ROOTFS, "${NERVES_SYSTEM}/images/rootfs.squashfs")

# This configuration file will create an image that has a GPT and the
# following layout on the eMMC. The boot ROM loads U-Boot from the fixed
# raw offset (sector 64); no separate boot flash is involved.
#
# +--------------------------------+
# | GPT                            |
# +--------------------------------+ <-- sector 64 (32 KB)
# | u-boot-rockchip.bin            |
# |  (TPL/DDR + SPL, FIT @ 8 MB)   |
# +--------------------------------+ <-- sector 30720 (15 MB)
# | Nerves firmware configuration  |
# | (formatted as U-Boot env)      |
# +--------------------------------+ <-- sector 32768 (16 MB)
# | p1: Boot partition (FAT32)     |
# |     extlinux/extlinux.conf     |
# |     Image.a, Image.b, dtb      |
# +--------------------------------+
# | p2: Rootfs A (squashfs)        |
# +--------------------------------+
# | p3: Rootfs B (squashfs)        |
# +--------------------------------+
# | p4: Application (f2fs)         |
# +--------------------------------+

# Mainline U-Boot's combined image is written at the boot ROM's fixed
# offset. Written only by the factory `complete` task -- upgrades never
# touch the bootloader.
define(UBOOT_IMG_OFFSET, 64)

# The Nerves U-Boot environment is written directly to the eMMC. It is not
# in any partition. NOTE: This must match rootfs_overlay/etc/fw_env.config
# and CONFIG_ENV_OFFSET in the U-Boot build (15 MB = 0xF00000, 128 KB).
# The FIT half of u-boot-rockchip.bin sits at 8 MB and ends well below.
define(UBOOT_ENV_OFFSET, 30720)
define(UBOOT_ENV_COUNT, 256)  # 128 KB

# The boot partition contains extlinux.conf, Image.a, Image.b and the dtb.
# 256 MB leaves room for two uncompressed arm64 kernel Images.
define(BOOT_PART_OFFSET, 32768)
define(BOOT_PART_COUNT, 524288)

# Let each rootfs have room to grow up to 512 MiB
define-eval(ROOTFS_A_PART_OFFSET, "${BOOT_PART_OFFSET} + ${BOOT_PART_COUNT}")
define(ROOTFS_A_PART_COUNT, 1048576)
define-eval(ROOTFS_B_PART_OFFSET, "${ROOTFS_A_PART_OFFSET} + ${ROOTFS_A_PART_COUNT}")
define(ROOTFS_B_PART_COUNT, ${ROOTFS_A_PART_COUNT})

# Application partition. This partition can occupy all of the remaining space.
# Size it to fit the destination.
define-eval(APP_PART_OFFSET, "${ROOTFS_B_PART_OFFSET} + ${ROOTFS_B_PART_COUNT}")
define(APP_PART_COUNT, 1048576)

# GPT identifiers. The rootfs GUIDs double as the root= kernel arguments
# (root=PARTUUID=...) in uboot/uboot.env and the extlinux configs, which
# keeps root selection independent of storage enumeration order.
define(DISK_UUID, "5b6e4afc-f230-42f2-80c0-42b92039eb26")
define(BOOT_PART_TYPE, "ebd0a0a2-b9e5-4433-87c0-68b6b72699c7")  # Microsoft basic data (FAT)
define(LINUX_PART_TYPE, "0fc63daf-8483-4772-8e79-3d69d8477de4") # Linux filesystem
define(BOOT_PART_GUID, "f047a708-d2da-4f60-961e-594c344d3a34")
define(ROOTFS_A_PART_GUID, "e7461250-672a-42ec-a738-0f38cf1e933d")
define(ROOTFS_B_PART_GUID, "187d8d14-28ac-429c-84cd-dc585a1b9b97")
define(APP_PART_GUID, "11033d99-0ca7-48a7-94ac-87df47556677")

# Firmware archive metadata
meta-product = ${NERVES_FW_PRODUCT}
meta-description = ${NERVES_FW_DESCRIPTION}
meta-version = ${NERVES_FW_VERSION}
meta-platform = ${NERVES_FW_PLATFORM}
meta-architecture = ${NERVES_FW_ARCHITECTURE}
meta-author = ${NERVES_FW_AUTHOR}
meta-vcs-identifier = ${NERVES_FW_VCS_IDENTIFIER}
meta-misc = ${NERVES_FW_MISC}
